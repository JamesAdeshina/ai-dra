import { CarerPagePlaceholder } from "@/features/carer/components/layout/carer-page-placeholder";

export default function CarerSettingsPage() {
  return (
    <CarerPagePlaceholder
      title="Carer Settings"
      description="Personal information, accessibility, notifications, privacy, permissions, password, and support settings will live here."
    />
  );
}
